@using RusBlazor.Components.Icon

@*
@inherits InputBase<bool>

<input type="checkbox" @bind="CurrentValue" @attributes="AdditionalAttributes" class="rus-checkbox"/>
*@

@if (Value)
{
    <RusIcon IconName="select_check_box" />
}
else
{
    <RusIcon IconName="check_box_outline_blank" />
}

@code {
    // TODO: Finish this component when its functionality is needed

    [Parameter]
    public bool Value { get; set; }

    @*
    protected override bool TryParseValueFromString(string value, out bool result, out string validationErrorMessage)
    {
        throw new NotImplementedException();
    }
    *@
}